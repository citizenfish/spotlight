"""Throwaway spikes.

Nothing in here is production code. Each spike exists to answer a question that
blocks the architecture, and is deleted once its answer is recorded in the design
vault. See the `spike`-labelled issues on citizenfish/spotlight.

Spikes build on `spotlight.core` and `spotlight.frontend` rather than
reimplementing them.
"""
